name: Auto Deploy to Render

on:
  push:
    branches:
      - master

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -X POST https://api.render.com/deploy/srv-d1jsu6e3jp1c73eoa690?key=Rps9RzZ0fxU